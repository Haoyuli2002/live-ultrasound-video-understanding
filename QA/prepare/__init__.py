"""
Clean data-preparation entry points for the QA pipeline.

This package wraps the older project scripts under `scripts/` so `QA/` can act
as the main clean pipeline directory without duplicating large legacy code.
"""